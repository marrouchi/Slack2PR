{
  "mcpServers": {
    "hexabot": {
      "type": "http",
      "url": "${HEXABOT_MCP_URL:-http://localhost:3000/api/mcp}",
      "headers": {
        "Authorization": "Bearer ${HEXABOT_MCP_TOKEN}"
      }
    }
  }
}